# Production CORS Configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:https://secretsanta.yourdomain.com}
app.cors.allowed-methods=GET,POST,OPTIONS
app.cors.allowed-headers=Content-Type,Authorization,X-Requested-With
app.cors.allow-credentials=false
app.cors.max-age=86400

# Production Settings
logging.level.com.balazs.hajdu.secretsanta=INFO
logging.level.org.springframework.web.cors=INFO